import { DecimalToFractionPipe } from './decimal-to-fraction.pipe';

describe('DecimalToFractionPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalToFractionPipe();
    expect(pipe).toBeTruthy();
  });
});
